VERSION=$1
# TODO set version and increase versioncode in gradle.properties
./gradlew clean assemble
git commit -a -m "Release version $VERSION"
git tag $VERSION
git remote | xargs -L1 git push
git remote | xargs -L1 git push --tags
scp app/build/outputs/apk/release/app-release-unsigned.apk flohmarkthelfer:www/shared/public/download/flohmarkthelfer.apk
